<UserControl x:Class="OrphanHousingService.Views.ContractsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:OrphanHousingService.Views.Controls"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <controls:SearchPanel Grid.Row="0"/>

        <DataGrid x:Name="ContractsDataGrid"
            Grid.Row="1"
            Style="{StaticResource ModernDataGrid}"
            AutoGenerateColumns="False"
            CanUserSortColumns="True"
            ItemsSource="{Binding Contracts}"
            SelectedItem="{Binding SelectedContract}"
            IsReadOnly="True"
            PreviewMouseRightButtonDown="ContractsDataGrid_PreviewMouseRightButtonDown">

            <DataGrid.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick"
                              Command="{Binding OpenDetailsCommand}" />
            </DataGrid.InputBindings>

            <DataGrid.ContextMenu>
                <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                    <MenuItem Header="Перейти к человеку"
                              Command="{Binding NavigateToPersonCommand}" />
                    <MenuItem Header="Перейти к квартире"
                              Command="{Binding NavigateToApartmentCommand}" />
                    <Separator />
                    <MenuItem Header="Добавить долг (UtilityDebt)"
                              Command="{Binding AddUtilityDebtCommand}" />
                    <MenuItem Header="Добавить члена семьи"
                              Command="{Binding AddFamilyMemberCommand}" />
                </ContextMenu>
            </DataGrid.ContextMenu>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Тип договора" Binding="{Binding DisplayContractType}"/>
                <DataGridTextColumn Header="Номер договора" Binding="{Binding ContractNumber}" />
                <DataGridTextColumn Header="Дата договора" Binding="{Binding ContractDate, StringFormat=dd.MM.yyyy}" />
                <DataGridTextColumn Header="Дата начала" Binding="{Binding StartDate, StringFormat=dd.MM.yyyy}" />
                <DataGridTextColumn Header="Дата окончания" Binding="{Binding EndDate, StringFormat=dd.MM.yyyy}" />
                <DataGridTextColumn Header="Статус" Binding="{Binding DisplayStatus}"/>
                <DataGridTextColumn Header="Гражданин" Binding="{Binding Person.FullName}" />
                <DataGridTextColumn Header="Квартира" Binding="{Binding Apartment.Address}" />
                <DataGridTextColumn Header="Предыдущий договор" Binding="{Binding PreviousContract.ContractNumber}" />
                <DataGridTextColumn Header="Долг" Binding="{Binding TotalDebt}" />
                <DataGridTextColumn Header="Добавлен" Binding="{Binding CreatedAt, StringFormat=dd.MM.yyyy HH:mm}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
