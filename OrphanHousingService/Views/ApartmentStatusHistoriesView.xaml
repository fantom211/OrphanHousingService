<UserControl x:Class="OrphanHousingService.Views.ApartmentStatusHistoriesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OrphanHousingService.Views"
        mc:Ignorable="d"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        >
    <Grid>
        <DataGrid  
            Style="{StaticResource ModernDataGrid}"
            IsReadOnly="True" AutoGenerateColumns="False"
            ItemsSource="{Binding Histories}"
            SelectedItem="{Binding SelectedHistory}"
            >

            <DataGrid.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick"
                  Command="{Binding OpenDetailsCommand}"
                  CommandParameter="{Binding SelectedHistory}" />
            </DataGrid.InputBindings>

            <DataGrid.Columns>

                <DataGridTextColumn
                Header="Квартира"
                Width="100"
                MaxWidth="150"
                Binding="{Binding Apartment.Address}" />

                <DataGridTextColumn
                Header="Дата изменения"
                Binding="{Binding ChangeDate}" />

                <DataGridTextColumn
                Header="Статус"
                Width="100"
                MaxWidth="150"
                Binding="{Binding DisplayStatus}" />

                <DataGridTextColumn
                    Header="Основание"
                    MaxWidth="300"
                    Width="200"
                    Binding="{Binding Basis}">

                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            <Setter Property="TextWrapping" Value="NoWrap"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="ToolTip" Value="{Binding Basis}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>

                </DataGridTextColumn>

                <DataGridTextColumn
                    Header="Комментарий"
                    MaxWidth="300"
                    Width="200"
                    Binding="{Binding Comment}">

                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            <Setter Property="TextWrapping" Value="NoWrap"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="ToolTip" Value="{Binding Comment}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>

                </DataGridTextColumn>

            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
