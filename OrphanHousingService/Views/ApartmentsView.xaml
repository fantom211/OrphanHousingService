<UserControl x:Class="OrphanHousingService.Views.ApartmentsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OrphanHousingService.Views"
        mc:Ignorable="d"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        >
    <Grid>
        <DataGrid 
            Style="{StaticResource ModernDataGrid}"
            IsReadOnly="True" AutoGenerateColumns="False" ItemsSource="{Binding Apartments}" SelectedItem="{Binding SelectedApartment}">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Header="Адрес"
                    Binding="{Binding Address}"/>
                <DataGridTextColumn
                    Header="Кадастровый номер"
                    Binding="{Binding CadastralNumber}"/>
                <DataGridTextColumn
                    Header="Площадь (м²)"
                    Binding="{Binding Area}"/>
                <DataGridTextColumn
                    Header="Кол-во комнат"
                    Binding="{Binding RoomsCount}"/>

                <DataGridTextColumn Header="Статус" Binding="{Binding StatusDisplay}"/>

                <DataGridTextColumn
                    Header="Дата включения в фонд"
                    Binding="{Binding IncludedToFundDate}"/>  
                <DataGridTextColumn
                    Header="Номер договора включения в фонд"
                    Binding="{Binding InclussionOrderNumber}"/>  
                <DataGridTextColumn
                    Header="Дата заключения договора"
                    Binding="{Binding InclussionOrderDate}"/>  
                   
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
