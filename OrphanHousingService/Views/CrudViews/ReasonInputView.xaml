<Window x:Class="OrphanHousingService.Views.CrudViews.ReasonInputView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behaviors="clr-namespace:OrphanHousingService.Behaviors"
        Title="{Binding Title}"
        Width="440"
        Height="220"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource DialogWindow}">
    <Border Margin="20" Style="{StaticResource CardBorder}" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding Prompt}"
                       FontSize="13"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="0,0,0,10"/>

            <TextBox Grid.Row="1"
                     Style="{StaticResource ModernMultilineTextBox}"
                     behaviors:TextBoxAutoGrowBehavior.MaxHeight="160"
                     Text="{Binding Reason, UpdateSourceTrigger=PropertyChanged}"/>

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,16,0,0">
                <Button Content="ОК"
                        Width="110"
                        Margin="0,0,10,0"
                        Command="{Binding ConfirmCommand}"/>
                <Button Content="Отмена"
                        Width="110"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding CancelCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
