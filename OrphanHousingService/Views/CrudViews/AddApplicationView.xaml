<Window x:Class="OrphanHousingService.Views.CrudViews.AddApplicationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AddApplicationView" Height="450" Width="800">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Номер заявки: "/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ApplicationNumber}"/>

        <TextBlock Grid.Row="1" Text="Договор:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="1"
              Grid.Column="1"
              ItemsSource="{Binding Contracts}"
              SelectedItem="{Binding SelectedContract}"
              IsEditable="True"
              IsTextSearchEnabled="True"
              DisplayMemberPath="ContractNumber"/>

        <TextBlock Grid.Row="2" Text="Тип заявки:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="2" Grid.Column="1"
                  ItemsSource="{Binding ApplicationTypes}"
                  SelectedValue="{Binding CurrentApplicationType}"
                  SelectedValuePath="Value"
                  DisplayMemberPath="Display"/>

        <TextBlock Grid.Row="3" Text="Дата заявки:" VerticalAlignment="Center"/>
        <DatePicker Grid.Row="3" Grid.Column="1" Margin="5"
                    SelectedDate="{Binding ApplicationDate}" />

        <TextBlock Grid.Row="4" Text="Статус:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="4" Grid.Column="1"
                  ItemsSource="{Binding Statuses}"
                  SelectedValue="{Binding CurrentStatus}"
                  SelectedValuePath="Value"
                  DisplayMemberPath="Display"/>

        <TextBlock Grid.Row="5" Text="Комментарий:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="5" Grid.Column="1" Margin="5"
                 Text="{Binding Comment}" />

        <StackPanel Grid.Row="6"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">

            <Button Content="Сохранить"
                    Width="120"
                    Margin="0,0,10,0"
                    Command="{Binding SaveCommand}" />

            <Button Content="Отмена"
                    Width="120"
                    Command="{Binding CancelCommand}" />

        </StackPanel>

    </Grid>
</Window>