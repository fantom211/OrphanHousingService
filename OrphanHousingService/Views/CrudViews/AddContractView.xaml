<Window x:Class="OrphanHousingService.Views.CrudViews.AddContractView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OrphanHousingService.Views.CrudViews"
        mc:Ignorable="d"
        Title="AddContractView"  
        Width="500"
        Height="300"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Person -->
        <TextBlock Text="Человек" Grid.Row="0"/>
        <ComboBox Grid.Row="0"
                  Grid.Column="1"
                  ItemsSource="{Binding People}"
                  SelectedItem="{Binding SelectedPerson}"
                  IsEditable="True"
                  IsTextSearchEnabled="True"
                  DisplayMemberPath="FullName"/>

        <!-- Apartment -->
        <TextBlock Text="Квартира" Grid.Row="1"/>
        <ComboBox Grid.Row="1"
                  Grid.Column="1"
                  ItemsSource="{Binding Apartments}"
                  SelectedItem="{Binding SelectedApartment}"
                  IsEditable="True"
                  IsTextSearchEnabled="True"
                  DisplayMemberPath="Address"/>

        <!-- ContractType -->
        <TextBlock Text="Тип" Grid.Row="2"/>
        <ComboBox Grid.Row="2"
                  Grid.Column="1"
                  ItemsSource="{Binding ContractTypes}"
                  SelectedValue="{Binding ContractType}"
                  SelectedValuePath="Value"
                  DisplayMemberPath="Display"/>

        <!-- Number -->
        <TextBlock Text="Номер договора" Grid.Row="3"/>
        <TextBox Grid.Row="3"
                 Grid.Column="1"
                 Text="{Binding ContractNumber}"/>

        <!-- Dates -->
        <TextBlock Text="Дата договора" Grid.Row="4"/>
        <DatePicker Grid.Row="4"
                     Grid.Column="1"
                     SelectedDate="{Binding ContractDate}"/>

        <TextBlock Text="Начало" Grid.Row="5"/>
        <DatePicker Grid.Row="5"
                     Grid.Column="1"
                     SelectedDate="{Binding StartDate}"/>

        <TextBlock Text="Конец" Grid.Row="6"/>
        <DatePicker Grid.Row="6"
                     Grid.Column="1"
                     SelectedDate="{Binding EndDate}"/>

        <TextBlock Text="Статус" Grid.Row="7"/>
        <ComboBox Grid.Row="7"
              Grid.Column="1"
              ItemsSource="{Binding ContractStatuses}"
              SelectedValue="{Binding Status}"
              SelectedValuePath="Value"
              DisplayMemberPath="Display"/>

        <StackPanel Grid.Row="8"
            Grid.ColumnSpan="2"
            Orientation="Horizontal"
            HorizontalAlignment="Right"
            Margin="0,15,0,0">

            <Button Content="Сохранить"
            Width="120"
            Margin="0,0,10,0"
            Command="{Binding SaveCommand}" />

            <Button Content="Отмена"
            Width="120"
            Command="{Binding CancelCommand}" />

        </StackPanel>


    </Grid>
</Window>
